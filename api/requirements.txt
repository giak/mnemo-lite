# Core dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1
python-dotenv>=1.0.1
# Added for SQLAlchemy Core with PostgreSQL
sqlalchemy>=2.0
psycopg2-binary
alembic>=1.13.0  # Database migrations with async support

# Monitoring
# prometheus-fastapi-instrumentator>=7.0.0 # Uncomment if needed

# Utilities
# httpx>=0.27.0 # Uncomment if external API calls needed
# requests>=2.31.0 # Uncomment if external API calls needed

# Logging
# loguru>=0.7.2 # Uncomment if using Loguru

# Security
# passlib[bcrypt]>=1.7.4 # Uncomment if needed for password hashing

# Optional database drivers (Keep only needed ones)
# asyncpg>=0.29.0 # If using async PostgreSQL with SQLAlchemy

# Development/Testing (Consider moving to requirements-dev.txt)
# pytest>=8.1.1
# pytest-asyncio>=0.23.6

# Add other dependencies below

uvloop==0.19.0
httptools==0.6.1
pydantic-settings==2.1.0
asyncpg>=0.29.0

# ============================================
# Machine Learning Dependencies (CPU-only)
# ============================================
# OPTIMIZATION: Force PyTorch CPU-only (no CUDA)
# Saves ~5 GB by removing nvidia CUDA libraries
# MnemoLite runs on CPU → CUDA unnecessary
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu

# Embeddings (installs after torch CPU to avoid CUDA version)
sentence-transformers>=2.7.0
numpy==1.26.3
einops>=0.7.0
jinja2==3.1.3
python-multipart==0.0.6
prometheus-client==0.18.0
tenacity==8.2.3
structlog==24.1.0
rich==13.7.0
psutil==5.9.6
pgvector

# EPIC-06 Phase 1: Code Intelligence
tree-sitter>=0.25.0  # Base library for AST parsing (updated for language-pack compatibility)

# EPIC-06 Phase 1.5: Multi-Language Support (JS/TS/PHP/Go/etc.)
# Using tree-sitter-language-pack for pre-compiled binaries (165+ languages)
# Replaces individual packages: tree-sitter-python, tree-sitter-javascript, tree-sitter-typescript
tree-sitter-language-pack>=0.4.0  # Pre-compiled language pack (Python, JS, TS, PHP, Go, etc.)

radon>=6.0.1  # Code complexity analysis (Story 3)

# EPIC-10 Story 10.2: L2 Redis Cache
redis[hiredis]>=5.0.0  # Async Redis client with hiredis for performance 