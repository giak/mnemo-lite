# Sample Markdown Document

## Introduction

This is a sample markdown document for testing chunking functionality.

## Code Examples

### Python Example

```python
def greet(name: str) -> str:
    """Return a greeting message."""
    return f"Hello, {name}!"

def calculate_sum(numbers: list) -> int:
    """Calculate sum of a list of numbers."""
    total = 0
    for num in numbers:
        total += num
    return total
```

### JavaScript Example

```javascript
function add(a, b) {
    return a + b;
}

const multiply = (a, b) => a * b;
```

## Features

### Feature One

This section describes feature one in detail.

- First item
- Second item
- Third item

### Feature Two

This section describes feature two with more details about the implementation.

#### Implementation Details

The implementation uses the following steps:

1. Initialize the system
2. Process the data
3. Return the result

#### Edge Cases

Consider these edge cases:
- Empty input
- Invalid data types
- Boundary conditions

## Advanced Topics

### Performance Optimization

For optimal performance, consider:

- Caching results
- Using lazy evaluation
- Parallel processing

### Error Handling

```python
try:
    result = risky_operation()
except ValueError as e:
    logger.error(f"Value error: {e}")
except Exception as e:
    logger.critical(f"Unexpected error: {e}")
```

## Conclusion

This document provides various structures for testing markdown chunking algorithms.